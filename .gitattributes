# סופי שורות — מדיניות אחת לכל הריפו (31.07.2026)
#
# בלי הקובץ הזה כל כלי כתב סופי שורות משלו, ולכן קובץ שנגעו בו הופיע
# ב-git כאילו נכתב מחדש כולו: `tests/e2e/smoke.spec.ts` הראה דיף של 2141
# שורות על שינוי של שורה אחת. זה הפך סקירת שינויים לבלתי אפשרית והיה
# מקור קבוע להתנגשויות. מעכשיו הריפו שומר LF תמיד, בכל מכונה.
* text=auto eol=lf

# תוצרים בינאריים — git לא נוגע בהם
*.pdf   binary
*.png   binary
*.jpg   binary
*.webp  binary
*.webm  binary
*.mp4   binary

# CLAUDE.md — הוראות חובה לפרויקט

## לפני כל פעולה
קרא לפי הסדר:
1. `USER_MEMORY.md`
2. `docs/CURRENT_REQUIREMENTS.md`
3. `docs/DECISION_LOG.md`

## גבול בטיחות קשיח
הפרויקט המותר היחיד הוא:
`yanivmizrachiy/coordinate-first-quadrant`

אסור לפתוח לצורך כתיבה, לשנות, להוסיף קבצים, לבצע commit או push אל:
`yanivmizrachiy/parabula-next`

אם `git remote get-url origin` אינו מצביע בדיוק אל
`yanivmizrachiy/coordinate-first-quadrant`
יש לעצור ולא לבצע כתיבה.

## מדיניות תיקונים מצטברת
- ההנחיה החדשה ביותר מחליפה רק כלל קודם שסותר אותה.
- אין למחוק, לקצר או לשנות דרישות אחרות.
- את הכלל המבוטל מעבירים לקטע „החלטות שהוחלפו” ב־`docs/DECISION_LOG.md`.
- את הכלל החדש מוסיפים ל־`USER_MEMORY.md` ול־`docs/CURRENT_REQUIREMENTS.md`.
- מבצעים שינוי ממוקד בלבד, ואז בדיקת רגרסיה מלאה.

## בדיקות חובה לפני שמירה
- קיימים עמודים 1–34.
- אפשר לגלול ולנווט לעמוד 34.
- אין חיתוך טקסט או צירים.
- קיימות תיבות תשובה ליד סרטוטים מתאימים.
- אין „ציר x” או „ציר y”; יש `x ציר` ו־`y ציר`.
- אין מילות דמו: שלב 3/4, ניתוח, טענה, אתגר.
- אין „תלמיד/תלמידה” ללא שם.
- אין שדות שם ותאריך.
- הכותרת התחתונה הקבועה נשמרת.
- התצוגה המקומית וגרסת GitHub Pages נפתחות.

## שמירה
לאחר שהבדיקות עוברות:
1. שמור את הקבצים.
2. אפשר לשירות `auto-sync-repo.ps1` לבצע commit ו־push אוטומטיים.
3. במקרה הצורך הפעל `sync-now.ps1`.

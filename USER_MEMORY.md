# מקור האמת היחיד — מערכת צירים: הרביע הראשון

עודכן: 2026-08-16

> זהו **מקור האמת היחיד** של הפרויקט. כל סוכן, אדם או כלי אוטומטי חייב לקרוא אותו לפני שינוי.
> `AGENTS.md`, `CLAUDE.md`, `README.md` ו־`HANDOFF.md` אינם מקורות כללים; הם רשאים רק להפנות לכאן או לתאר מצב.
> היסטוריית Git משמרת החלטות ישנות, אך **היסטוריה אינה כלל פעיל**.
> הקוד, הבדיקות והיסטוריית Git משמשים **ראיות למצב המימוש בלבד**; הם לעולם אינם מקור דרישות חלופי למסמך הזה.

## 1. קדימות וכלל ניקיון

1. הוראה מפורשת חדשה של יניב גוברת על כלל ישן שסותר אותה.
2. כשכלל הוחלף — **מוחקים או משכתבים את הישן**. לא משאירים שתי גרסאות פעילות ולא מוסיפים שכבות של „הוחלף” בתוך מקור האמת.
3. אם קוד, בדיקה, README, HANDOFF או תיאור אחר סותרים את המסמך הזה — בודקים את המצב האמיתי ומיישרים אותם אליו; לא משנים את מקור האמת כדי להצדיק קוד ישן. קוד/בדיקה/היסטוריית Git אינם רשאים לעקוף כלל פעיל כאן. החריג היחיד הוא הוראה מפורשת חדשה של יניב, שאותה יש קודם לעדכן כאן ואז ליישר אליה את המימוש.
4. אין להמציא דרישה, עיצוב, מספר עמודים, פיצ'ר או כלל שלא מופיע במסמך הזה או נמסר בהוראה מפורשת חדשה של יניב.
5. כל דרישה שחוזרת או עלולה להישבר שוב מקבלת בדיקה אוטומטית כאשר הדבר אפשרי.
6. לפני שינוי בודקים מה כבר קיים. **לא מתחילים מחדש עבודה שכבר בוצעה נכון.**

## 2. זהות הפרויקט והמצב הקנוני

- הריפו היחיד: `yanivmizrachiy/coordinate-first-quadrant`.
- ענף הייצור: `main`.
- נקודת הבסיס שנבדקה בעת ניקוי מקור האמת: `aa27ee705a29fbd3b085aca1f8bfbc06b0b95ac7` (02.08.2026). לפני עבודה חדשה יש לבדוק את `HEAD` בפועל ולא להניח שהגיבוב הזה עדיין האחרון.
- טכנולוגיה: **Vite + TypeScript**, אתר סטטי אחד, `index.html` אחד.
- **החוברת = 78 עמודים ממוספרים**.
- בנוסף לחוברת: **עמוד שער + עמוד תוכן עניינים**. לכן החוברת המלאה להדפסה היא **80 גיליונות A4**.
- `#/print-aids` הוא אזור **המחשות נפרד** להדפסה ואינו נספר בתוך 78 העמודים או בתוך 80 גיליונות החוברת.
- אין משימות מקוונות בעמודים הממוספרים. כל משימה ממוספרת חייבת להיות פתירה בעיפרון ועל נייר.
- דפי חידה/פענוח קיימים כחלק מהחוברת, אך הם **דפי עבודה מודפסים**, לא ווידג'טים מקוונים.
- ארבעה דפי פוסטר מבוססים על אמנות תמונה מוכנה; הם חלק מהעמודים הממוספרים.

### נתיבים פעילים

- `#/` — דף הבית/הפתיחה של האתר.
- `#/menu` — תפריט הפעולות.
- `#/book` — החוברת המדפדפת.
- `#/print` — כל החוברת ברצף A4 לצורך הדפסה, PDF ומדידות.
- `#/workbook/:n` — עמוד בודד.
- `#/print-aids` — המחשות להדפסה.
- נתיבים ישנים כגון `#/games` אינם מגדירים אזור משחקים חדש; אם נשמר redirect תאימות, הוא מפנה לחוברת בלבד.

## 3. גבול בטיחות, Git ופרסום

- **אסור לכתוב** אל `yanivmizrachiy/parabula-next`. קריאה ממנו מותרת רק אם יניב ביקש במפורש לשאוב חומר, ולעולם לא כותבים אליו.
- לפני כל כתיבה: מאמתים remote/repo, ענף, `git status` והעבודה האחרונה. אין לסחוף שינויים זרים לקומיט.
- עבודה מקבילה על אותו checkout מסוכנת. אם קיימת עבודה לא־קשורה של סוכן/שיחה אחרת — לא דורסים אותה.
- סופי שורות: LF; `.gitattributes` נשאר.
- ליניב יש אישור עומד בתוך הריפו הזה ל־commit, push, PR, merge ל־`main` ופרסום, **בתנאי שכל שערי האימות הרלוונטיים ירוקים**.
- הקישור הרשמי היחיד שמפרסמים למשתמשים הוא:
  `https://yanivmizrachiy.github.io/coordinate-first-quadrant/`
- GitHub Pages הוא הכתובת הרשמית. פריסת Pages היא ידנית דרך workflow הייעודי לאחר אימות.
- Vercel רשאי לשמש תשתית פריסה/בדיקה של אותו אתר, אך אינו קישור ציבורי נוסף של הפרויקט.
- אין לשכתב היסטוריית Git או לבצע force-push רק כדי להקטין את הריפו.

## 4. ארכיטקטורה וקוד

- מקור כל עמוד: `src/data/workbook/pages/` — **קובץ אחד לכל עמוד**, בשם שמתאר את הנושא.
- `src/data/workbook/index.ts` והמבנה `BOOK` הם מקור הסדר, המספור ושיוך הפרקים. מספרי עמודים אינם נכתבים ידנית ב־CSS או בלוגיקה.
- **אסור** סלקטור/לוגיקה מסוג `#page-N` שתלויים במספר עמוד משתנה.
- מעטפת משותפת (`sheet()`, כותרת, מספר, footer וכלי authoring) נכתבת פעם אחת ומשותפת לכל הדפים.
- משתמשים בכלים המשותפים לשרטוט, זוג סדור, תרגיל וחישוב; אין לשכפל ידנית markup שכבר קיים לו helper.
- ליבת החוברת אינה תלויה ברשת. חריג מכוון: סרטון עדכון תכנית הלימודים בדף הנחיתה נטען מ־YouTube במצב privacy-enhanced ורק לאחר פעולת משתמש.
- נכסים מקומיים, גופנים, סרט הפתיחה, פוסטרים ו־PDF נשמרים בריפו לפי המבנה הקיים.
- שינוי בפוסטר PNG מחייב סנכרון/יצירה מחדש של תאום ה־WebP שהדפדפן משתמש בו.

## 5. עקרונות פדגוגיים ותוכן

- תחום הלימוד: **הרביע הראשון בלבד**, כולל הצירים וראשית הצירים. אין לעבור למספרים שליליים.
- סדר הלמידה מדורג: זיהוי לפני יצירה; קריאה לפני סימון; מיומנות בסיסית לפני יישום מורכב.
- כל עמוד מלמד מטרה מרכזית ברורה. משימות בעמוד צריכות לתרגל את מה שהעמוד מלמד ולא לקפוץ למיומנות אחרת.
- התלמיד אינו רק מעתיק תשובה קיימת: היכן שמתאים מבקשים ממנו לסמן/לבחור נקודה משלו, לכתוב את הזוג הסדור שבחר ואז לענות שאלה שנגזרת מבחירתו.
- אין חזרות משעממות שבהן רק המספרים משתנים. בכל מקבץ החסר/המיומנות משתנים באופן מכוון.
- הוראה נכתבת פעם אחת. אין תיבת כלל שאומרת פעולה ואז כותרת סמוכה שחוזרת על אותה פעולה.
- אין מילות דמו, שיווק או תיאור עצמי של המערכת. טקסט באתר ובחוברת צריך למסור עובדה, הוראה או פעולה אמיתית.
- הקשרים מחיי התלמיד חייבים להיות המתמטיקה עצמה, לא קישוט מסביב לתרגיל.

## 6. עברית, ניסוח וסימון מתמטי

- כל הממשק והחוברת בעברית RTL; ביטויים מתמטיים, חיסורים, זוגות סדורים ושמות קטעים מוצגים LTR במקום הנדרש.
- כותבים **`ציר x` / `ציר y`**, לא `x ציר` / `y ציר`.
- לפני אות של נקודה כותבים **„הנקודה”**: „הנקודה A”, „הנקודה B”.
- לתיאור מקום משתמשים ב־**„ממוקם/ממוקמת”** ולא „נמצא/נמצאת” בטקסט חדש. טקסט צרוב בתוך פוסטר ישן שאי אפשר לערוך באותו גופן אינו סיבה לעצב מחדש את האמנות.
- זוג סדור נכתב בסדר `x,y`; ה־x הוא הרכיב הראשון וה־y השני. אין `A = (x,y)`; כותבים `A(x,y)`.
- כששיעור מייצג גודל מהחיים כותבים **„מייצג”**, לא „הוא”: למשל שיעור x מייצג משקל.
- משתמשים בספרות ובשברים טיפוגרפיים של ספר לימוד, לא במספרים עשרוניים במקום שבר כשלא נדרש.
- כללי פיסוק והגהה חלים בכל שינוי: אין רווח לפני סימן פיסוק, יש רווח אחריו, מקף עברי לפני אות/מספר במקום המתאים, ואין רווחים כפולים.
- ברירת המחדל היא משפטי השלמה קצרים וברורים במקום שאלות פתוחות ארוכות מסוג „הסבירו/נמקו”.
- חריג מאושר בדפי גילוי: בסיום מותר לשאול בקצרה **„מה קיבלתם?”** בלי לחשוף את התשובה מראש.
- מחסן מילים משמש כאשר הוא באמת מסייע להשלמה; אין להוסיף מחסן שמגלה את תשובת דף הגילוי או משכפל מילים שכבר מודפסות מעליו.
- בטענות משתמשים בניסוחים: **„נכונה בהכרח” / „ייתכן שנכונה” / „לא ייתכן”**, ולא „תמיד/לפעמים/לעולם לא”.

## 7. מתמטיקה קנונית

- שיעור x הוא המרחק מציר y; שיעור y הוא המרחק מציר x — ברביע הראשון.
- קטע מקביל לציר x: האורך מתקבל מהפרש שיעורי x. קטע מקביל לציר y: האורך מתקבל מהפרש שיעורי y.
- אורך מחושב בתרגיל חיסור; לא מלמדים לספור משבצות כדרך העיקרית.
- במלבן: **אורך** הוא הצלע הארוכה ו**רוחב** הצלע הקצרה; אין להחליף זאת ב„צלע אופקית/אנכית” כשמדברים על מידות המלבן.
- חישובים נכתבים משמאל לימין.
- `P` מסמנת היקף ו־`S` מסמנת שטח.
- הדרך לחישוב חשובה: משתמשים ב־`calcBox()`/helpers הקיימים, משאירים מקום אמיתי לדרך, ומסיימים בתשובה אחת בלבד עם יחידה.
- אורך/היקף: יחידות אורך; שטח: יחידות ריבועיות.
- אין נוסחת אמצע קטע ואין נוסחת מרחק כללית בין שתי נקודות — מחוץ להיקף היחידה.
- נקודה/קטע/מצולע לעולם אינם מצוירים מחוץ לטווח הצירים של השרטוט שלהם.

## 8. עיצוב, A4, שרטוטים ומובייל

- כל עמוד ממוספר הוא **A4 אמיתי** ומודפס כגיליון אחד בדיוק.
- 78 עמודים + שער + תוכן עניינים = **80 עמודי PDF**. אין עמודים ריקים נוספים.
- אין חיתוך תוכן. אם עמוד צפוף — מפצלים/משנים תוכן; לא מקטינים עד חוסר קריאות.
- גוף הטקסט בגיליון נשמר בגודל 13px לפי המנגנון והחריגים המכוונים הקיימים.
- שרטוטים צריכים להיות גדולים וברורים; אין יותר משני שרטוטים בשורה; שרטוט שדורש קריאה/כתיבה אינו יורד מתחת לרוחב השימושי שנאכף בבדיקות.
- שמות הצירים, נקודות, קודקודים וחצים אינם מתנגשים זה בזה ואינם מסתירים מספרי ציר.
- סימן זווית ישרה בראשית הצירים מופיע כברירת מחדל כאשר הוא מתאים לשרטוט.
- footer קנוני בכל עמוד:
  - `יניב רז - מדריך מחוזי חט"ב בעיר ירושלים`
  - `הדרכה במחוז ירושלים והעיר ירושלים - מנח"י, בהובלת איילת קריספין`
- הקו מעל ה־footer משתמש בכחול הכותרת הראשית (`var(--brand)`); תוכן העניינים שומר את שפת השמנת/זהב המאושרת שלו.
- עיצוב תוכן העניינים הקיים **נעול**: אין לפתוח מחדש את בחירת הסגנון בלי הוראה חדשה מפורשת מיניב.
- עיצוב תיבת החישוב הקיים **נעול**: מחברת משבצות, P/S בולטים, דרך ותשובה מסודרות.
- בטלפון רואים את **אותו A4 מוקטן**, לא גרסה שזורמת מחדש ושוברת עמודות. התאמה למובייל היא scaling חזותי ושמירת יחס A4.
- המסך מוצג בצבע. שחור־לבן הוא מצב של הורדה/הדפסה, לא מצב קבוע של הממשק.

## 9. חוברת, תוכן עניינים, הורדה והדפסה

- `#/book` הוא קורא החוברת המדפדפת; `#/print` הוא מקור ההדפסה והמדידה.
- „התחל” בכניסה לחוברת פותח את תוכן העניינים, לא עמוד אחרון שנשמר בעבר.
- תוכן העניינים הוא יחיד, אינו ממוספר, ומפנה לפרקי החוברת. שמות ועמודי התחלה נגזרים מהמבנה הקנוני ולא מתוחזקים כעותק שני סותר.
- אין אזור נפרד של משחקים מקוונים.
- הורדה והדפסה הן פעולות נפרדות, אך משתמשות בבוחר נוח של היקף (הכול/נבחרים) וצבע (צבע/שחור־לבן) לפי המימוש הקיים.
- PDF צבע: `public/hoveret.pdf`; PDF שחור־לבן: `public/hoveret-bw.pdf`; מפת עמודים: `public/hoveret-map.json`.
- אחרי **שינוי שנראה בעמוד** (טקסט, שרטוט, פריסה, פוסטר) מריצים `npm run pdf`, מאמתים 80 עמודים ושכל גיליון ממופה לעמוד A4 אחד, ומכניסים את הארטיפקטים המעודכנים לקומיט.
- אחרי שינוי שאינו משנה רינדור אין לייצר PDF חדש רק כדי לשנות בתים בקובץ.
- כל media query של מובייל שעלול לחול גם על רוחב A4 חייב להיות מוגבל ל־`screen` כאשר זה נדרש, כדי לא לזהם את ה־PDF.

## 10. פוסטרים והמחשות

- פוסטר מקור מוכן אינו מעוצב מחדש ללא צורך. אין למתוח, לחתוך או לשנות צבע באופן שמפר את האמנות המאושרת.
- לפוסטרים אין שדות שם/כיתה/תאריך.
- פוסטר כחלק מהחוברת מקבל מספר עמוד ו־footer לפי המנגנון הקנוני.
- שינוי אמיתי ב־PNG מחייב יצירת WebP תואם ובדיקת התאמה.
- `#/print-aids` הוא מסלול נפרד להמחשות A4. ההמחשה הקיימת „צירים ליצירת הרביע הראשון” נשארת נפרדת ממספור החוברת.

## 11. בדיקות ושער סיום

פקודת השער הראשית:

```bash
npm run verify
```

היא חייבת לכלול typecheck, בדיקות יחידה/תוכן, build ובדיקות דפדפן/רינדור. CI חייב להישאר ירוק בכל push/PR.

לפני הכרזה „סיימתי” מאמתים לפחות:

- 78 עמודים ממוספרים, ללא חורים וללא כפל.
- 80 גיליונות בחוברת המודפסת, ללא עמודים ריקים וללא גלישה.
- אף עמוד ממוספר אינו מכיל `button`/`select`/`textarea` של משימה מקוונת.
- כל HTML/markup של עמוד מאוזן.
- כל נקודה/קטע/מצולע נמצאים בטווח הצירים שלהם.
- אין חפיפות/חיתוכים/טקסט זעיר/גלילה אופקית לא רצויה.
- תצוגת mobile שומרת על אותו A4.
- PDF/מפת PDF מעודכנים כאשר היה שינוי נראה.
- PNG/WebP מסונכרנים כאשר פוסטר השתנה.
- README, `package.json`, `index.html`, `HANDOFF.md`, `AGENTS.md` ו־`CLAUDE.md` אינם מציגים עובדות ישנות שסותרות את מקור האמת.
- אין בקבצים הפעילים תיאור מצב שסותר את הספירה הקנונית של **78 עמודים ממוספרים ו־80 גיליונות מלאים**, ואין בהם טענה שמציגה שעשועונים אינטראקטיביים כחלק פעיל מהחוברת. היסטוריית Git בלבד רשאית לשמר מצבים ישנים.

## 12. פרוטוקול עבודה לסוכן עד סיום

1. לקרוא את המסמך הזה במלואו.
2. לבדוק `main` והקוד בפועל; לא להסתמך על זיכרון, HANDOFF ישן או מספרי עמודים ישנים.
3. לסרוק לפני עריכה כדי להבין מה כבר תקין ומה באמת סותר.
4. לתקן **רק כשיש ראיה**, אבל כשמתגלה כלל רוחבי — לתקן אותו בכל המופעים ולהוסיף guard מתאים.
5. לא להכניס שדרוגי עיצוב או פיצ'רים שלא התבקשו תוך כדי „ניקוי”.
6. לשמור את מקור האמת נקי: שינוי חדש מחליף את הישן במקום להיערם מעליו.
7. להריץ את שערי האימות; אם נכשלו — לתקן ולא לפרסם.
8. בשינוי נראה לעין: לחדש PDF/נכסים נגזרים הרלוונטיים ולאמת אותם.
9. commit/push/merge/Pages רק לאחר שהכול ירוק.
10. לדווח עובדות: מה השתנה, אילו בדיקות עברו, commit מדויק וקישור הייצור. אין לכתוב „הכול הושלם” אם שלב כלשהו לא אומת.
11. לעבוד **בעוצמת היכולות המרבית הזמינה בסביבת הסוכן**: להשתמש במצב reasoning/analysis האיכותי ביותר הזמין, חיפוש רוחבי בריפו, static analysis, typecheck, בדיקות אוטומטיות, Playwright/דפדפן, בדיקות חזותיות, כלים ל־PDF, Git/GitHub Actions וכלי אימות נוספים כאשר הם רלוונטיים. אין להוריד במכוון את רמת הבדיקה או להסתפק במדגם כשאפשר לבצע בדיקה מלאה.
12. כאשר קיימים agents/subagents/parallel workers או worktrees בטוחים — מותר ואף רצוי להשתמש בהם במקביל למשימות **קריאה ואימות עצמאיות**, אך כל המסקנות מתכנסות למסלול אינטגרציה אחד ולמקור אמת יחיד. אין לתת לסוכן משנה להמציא מקור כללים נפרד.
13. „עבודה בעוצמה מרבית” אינה רישיון להחליף stack, לעצב מחדש או להוסיף טכנולוגיה חדשה ללא צורך. נשארים עם Vite + TypeScript והחלטות העיצוב הנעולות, אלא אם הוראה חדשה מפורשת של יניב משנה זאת.
14. כל סוכן וסוכן משנה חייב לקבל את `USER_MEMORY.md` כמקור הדרישות הנורמטיבי היחיד. קוד, tests, README, HANDOFF, CLAUDE, AGENTS והיסטוריית Git יכולים לספק ראיות והקשר — **לא דרישות מתחרות**.

---

**עיקרון העל:** מקור אמת טוב אינו יומן. הוא מתאר רק מה שנכון עכשיו. ההיסטוריה נשארת ב־Git; כאן נשארת אמת אחת, קצרה, מחייבת וללא סתירות.

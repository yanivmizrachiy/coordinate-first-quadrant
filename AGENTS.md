# AGENTS.md — הוראות לסוכני קוד / Codex

זהו **שלט הפניה ותפעול בלבד**. מקור האמת היחיד של הפרויקט הוא:

> `USER_MEMORY.md`

קרא אותו במלואו לפני כל שינוי.

## מקור אמת קשיח — אין מקור שני

- `USER_MEMORY.md` הוא **המקור הנורמטיבי היחיד** לדרישות, כללים והחלטות הפרויקט.
- קוד, tests, README, HANDOFF, CLAUDE, AGENTS והיסטוריית Git הם **ראיות למצב המימוש והקשר בלבד**. הם אינם רשאים לעקוף כלל פעיל ב־`USER_MEMORY.md` ואינם מקור אמת חלופי.
- אם המימוש סותר את `USER_MEMORY.md` — המימוש נחשב חשוד/שגוי עד שייושר, לא להפך.
- החריג היחיד: הוראה מפורשת חדשה של יניב. במקרה כזה מעדכנים תחילה את `USER_MEMORY.md`, מוחקים/משכתבים את הכלל שסותר אותה, ורק אחר כך מיישרים קוד, tests ותיעוד.
- אין לייצר דף כללים נוסף, decision source נוסף או "אמת זמנית" אצל סוכן משנה.

## מצב עבודה — עוצמה מרבית

עבוד באמצעות **היכולות החזקות ביותר הזמינות בפועל בסביבת Codex**. אל תבצע downgrade מכוון ואל תסתפק בבדיקה שטחית כאשר ניתן לבצע אימות מלא.

כאשר רלוונטי, השתמש בשילוב של:

- reasoning/analysis ברמה הגבוהה ביותר הזמינה;
- חיפוש רוחבי ומובנה בכל הריפו;
- static analysis ו־TypeScript typecheck;
- unit/content/regression tests;
- Playwright/דפדפן אמיתי ובדיקות E2E;
- visual regression, screenshots ומדידות DOM/CSS;
- בדיקות A4/PDF ומספר עמודים;
- Git history רק להבנת מימוש והקשר;
- GitHub Actions וסטטוסי CI;
- agents/subagents/parallel workers או worktrees בטוחים למשימות קריאה ואימות עצמאיות כאשר הם זמינים ומועילים.

אם משתמשים בסוכני משנה, כולם חייבים לקבל את `USER_MEMORY.md` כמקור הדרישות היחיד, לעבוד על תחומים שאינם מתנגשים, ולהחזיר ממצאים למסלול אינטגרציה אחד. אל תאפשר להם ליצור כללים חדשים בעצמם.

**עוצמה מרבית אינה רישיון להמציא פיצ'רים, להחליף stack או לעצב מחדש.** נשארים עם Vite + TypeScript והחלטות הנעולות במקור האמת, אלא אם יניב נתן הוראה חדשה מפורשת.

## גבול בטיחות

- הריפו היחיד שמותר לכתוב אליו: `yanivmizrachiy/coordinate-first-quadrant`.
- אסור כל write / commit / push / merge אל `yanivmizrachiy/parabula-next`.
- לפני כתיבה: אמת remote, ענף, `git status` ו־HEAD.
- אל תדרוס עבודה זרה ואל תכניס לקומיט שינויים לא־קשורים.

## משימת Codex הנוכחית — לקדם את הפרויקט עד מצב סיום אמיתי

אל תתחיל מחדש ואל תניח שהמסמכים הישנים נכונים. עבוד מהמצב החי של `main`.

1. קרא `USER_MEMORY.md` במלואו.
2. בצע audit רוחבי של הקוד, הבדיקות והתיעוד מול מקור האמת.
3. חפש במיוחד שרידים של מצבים ישנים והסר רק שרידים אמיתיים:
   - כל ספירת עמודים שאינה **78 עמודים ממוספרים**;
   - כל ספירת חוברת מלאה שאינה **80 גיליונות** כולל שער ותוכן עניינים;
   - טענות שמציגות שעשועונים אינטראקטיביים כחלק פעיל מהעמודים הממוספרים;
   - קוד/בדיקות/מלל ישנים שמניחים game widgets בתוך החוברת;
   - כללים ישנים על מסך פתיחה, כפתור קול או זרימת ניווט שכבר הוחלפו;
   - תיעוד שסותר את המצב הקנוני ב־`USER_MEMORY.md`.
4. **אל תמחק קוד רק מפני שמחרוזת ישנה נמצאה.** אמת שהוא באמת לא בשימוש ושאינו redirect/תאימות מכוונת.
5. בדוק את כל 78 העמודים מתמטית, לשונית וחזותית באמצעות הבדיקות והמנגנונים הקיימים; אל תמציא עיצוב חדש אם הקיים עומד בכללים.
6. בעיה רוחבית מתקנים במקור המשותף ומוסיפים regression test, לא patch נקודתי לעמוד אחד.
7. שמור על החלטות נעולות: תוכן העניינים, תיבת החישוב, מסך צבעוני/שחור־לבן רק בהדפסה, חוברת מדפדפת מול `#/print`, ואין משחקים מקוונים בעמודים ממוספרים.
8. אם שינוי נראה בעמוד — הרץ `npm run pdf`, אמת 80 עמודי PDF ללא עמודים ריקים ועדכן את הארטיפקטים. אם פוסטר PNG השתנה — צור גם WebP תואם.
9. הרץ בסוף:

```bash
npm run verify
```

10. אל תכריז סיום לפני שכל הבדיקות הרלוונטיות ירוקות, הקוד והתיעוד מסונכרנים, generated artifacts מעודכנים כשצריך, והמצב נשמר ב־Git.
11. ליניב יש אישור עומד ל־commit/push/PR/merge/פרסום בריפו הזה לאחר שערים ירוקים. אם סביבת Codex מאפשרת — סגור את המעגל עד `main` ו־GitHub Pages. אם פעולה מסוימת חסומה טכנית, דווח במדויק מה חסום ומה כבר הושלם; אל תכתוב שסיימת.

## כלל מקור אמת

`USER_MEMORY.md` אינו יומן. אל תוסיף אליו תיאור ארוך של מה שעשית. עדכן אותו רק כאשר הוראה חדשה של יניב משנה את האמת המחייבת, ובמקרה כזה מחליפים את הכלל הישן במקום להשאיר שתי גרסאות.

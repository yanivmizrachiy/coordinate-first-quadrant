# AGENTS.md — הוראות לסוכני קוד / Codex

זהו **שלט הפניה ותפעול בלבד**. מקור האמת היחיד של הפרויקט הוא:

> `USER_MEMORY.md`

קרא אותו במלואו לפני כל שינוי. אם קובץ אחר סותר אותו — הקובץ האחר מיושן עד שיוכח אחרת בבדיקת הקוד והיסטוריית Git.

## גבול בטיחות

- הריפו היחיד שמותר לכתוב אליו: `yanivmizrachiy/coordinate-first-quadrant`.
- אסור כל write / commit / push / merge אל `yanivmizrachiy/parabula-next`.
- לפני כתיבה: אמת remote, ענף, `git status` ו־HEAD.
- אל תדרוס עבודה זרה ואל תכניס לקומיט שינויים לא־קשורים.

## משימת Codex הנוכחית — לקדם את הפרויקט עד מצב סיום אמיתי

אל תתחיל מחדש ואל תניח שהמסמכים הישנים נכונים. עבוד מהמצב החי של `main`.

1. קרא `USER_MEMORY.md`.
2. בצע audit רוחבי של הקוד, הבדיקות והתיעוד מול מקור האמת.
3. חפש במיוחד שרידים של המצבים הישנים והסר רק שרידים אמיתיים:
   - `34` עמודים;
   - `77` עמודים / `79` גיליונות;
   - טענות שיש „שעשועונים אינטראקטיביים” בעמודים הממוספרים;
   - קוד/בדיקות/מלל ישנים שמניחים game widgets בתוך החוברת;
   - כללים ישנים על מסך פתיחה, כפתור קול או זרימת ניווט שכבר הוחלפו;
   - תיעוד ישן שסותר 78 עמודים ממוספרים + שער + תוכן עניינים = 80 גיליונות.
4. **אל תמחק קוד רק מפני שמחרוזת ישנה נמצאה.** אמת שהוא באמת לא בשימוש ושאינו redirect/תאימות מכוונת.
5. בדוק את כל 78 העמודים מתמטית, לשונית וחזותית באמצעות הבדיקות והמנגנונים הקיימים; אל תמציא עיצוב חדש אם הקיים עומד בכללים.
6. בעיה רוחבית מתקנים במקור המשותף ומוסיפים regression test, לא patch נקודתי לעמוד אחד.
7. שמור על החלטות נעולות: תוכן העניינים, תיבת החישוב, מסך צבעוני/שחור־לבן רק בהדפסה, חוברת מדפדפת מול `#/print`, ואין משחקים מקוונים בעמודים ממוספרים.
8. אם שינוי נראה בעמוד — הרץ `npm run pdf`, אמת 80 עמודי PDF ללא עמודים ריקים ועדכן את הארטיפקטים. אם פוסטר PNG השתנה — צור גם WebP תואם.
9. הרץ בסוף:

```bash
npm run verify
```

10. אל תכריז סיום לפני שכל הבדיקות הרלוונטיות ירוקות, הקוד והתיעוד מסונכרנים, generated artifacts מעודכנים כשצריך, והמצב נשמר ב־Git.
11. ליניב יש אישור עומד ל־commit/push/PR/merge/פרסום בריפו הזה לאחר שערים ירוקים. אם סביבת Codex מאפשרת — סגור את המעגל עד `main` ו־GitHub Pages. אם פעולה מסוימת חסומה טכנית, דווח במדויק מה חסום ומה כבר הושלם; אל תכתוב שסיימת.

## כלל מקור אמת

`USER_MEMORY.md` אינו יומן. אל תוסיף אליו תיאור ארוך של מה שעשית. עדכן אותו רק כאשר הוראה חדשה של יניב משנה את האמת המחייבת, ובמקרה כזה מחליפים את הכלל הישן במקום להשאיר שתי גרסאות.
